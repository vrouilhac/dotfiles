# alias lsa='ls -A -1 -G'
alias soh='exec $SHELL'
alias history='history 0'
alias hist='history 0 | fzf'
alias lua='lua5.1'

so() {
	source $MYZSHRC
	echo 'zsh reloaded!'
}

z() {
  __zoxide_z "$@"
}
